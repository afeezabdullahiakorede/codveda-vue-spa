# A Single Page Application (SPA) using Vue 3 and Vite. 

## Key Milestones:
🔹 Configured Vue Router dynamically to map paths (/about, /contact) without browser reloads.
🔹 Managed dynamic viewport rendering using <router-view /> and nested layout logic in App.vue.
🔹 Compiled optimized production-ready assets using Vite’s build pipeline.

